name: Swift

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  DEVELOPER_DIR: /Applications/Xcode_26.2.app/Contents/Developer

jobs:
  swift-macos:
    name: Swift (macOS)
    runs-on: macos-26
    timeout-minutes: 30
    steps:
    - name: Checkout source
      uses: actions/checkout@v6
    - name: Swift version
      run: swift --version
    - name: Build
      run: swift build -v
    - name: Test
      run: swift test -v

  swift-linux:
    name: Swift (Linux)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # ubuntu-latest images no longer ship a Swift toolchain; run in the
    # official Swift container instead. The swift-tools-version:6.3
    # manifest falls back to Package@swift-5.9.swift on older toolchains.
    container: swift:6.1
    steps:
    - name: Checkout source
      uses: actions/checkout@v6
    - name: Swift version
      run: swift --version
    - name: Build
      run: swift build -v
    - name: Test
      run: swift test -v
